server {
  listen 80;
  server_name localhost;

  location /static/ {
    alias /app/static;
  }

  location / {
    proxy_pass http://python_app:80;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $host;
    proxy_redirect off;
  }
}
